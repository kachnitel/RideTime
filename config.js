// Use this for local development
// (Assigns local network address)
// import { Constants } from 'expo'
// const { manifest } = Constants
// export const apiUrl = 'http://' + ((typeof manifest.packagerOpts === `object`) && manifest.packagerOpts.dev
//   ? manifest.debuggerHost.split(`:`).shift().concat(`:80`)
//   : `apiUrl.example.com`) + '/ridetime'

export const apiUrl = 'https://ridetime.tk'
